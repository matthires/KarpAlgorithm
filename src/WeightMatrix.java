
/**
 * The class represents the adjacency matrix of a graph.The values are the weights each edge has.
 * @see Matrix
 */
public class WeightMatrix extends Matrix{

	public WeightMatrix(int dimension) {
		super(dimension);
	}
	
}
